* You can use ! in an HTML file to generate a HTML template

* You can tyoe font awesome CDN in google to get taken to a JS script you can put in html

* You can type (.)Name to generate a div with a classname